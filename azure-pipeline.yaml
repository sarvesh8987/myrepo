trigger:
  - master

pool:
  vmImage: 'ubuntu-latest'

steps:
  - script: |
      echo "This is a demo YAML file for DevSecOps Pipeline"
    displayName: "Print console Text for Demo 1 / first change"
 
